"""agentcast — asciinema for AI coding agents.

Turn any Claude Code or Codex CLI session already on your disk into a
self-contained, shareable HTML replay: every prompt, every file read,
every edit as a diff, every command, tokens, cost, blast radius.

Pure Python standard library. No dependencies. Nothing leaves your machine.
"""
__version__ = "0.1.0"
